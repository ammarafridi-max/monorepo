# Fly.io config for dt365-backend
# Deploy from monorepo root with: flyctl deploy -c fly.dt365-backend.toml

app = "dt365-backend"
primary_region = "fra"

[build]
  dockerfile = "apps/dt365-backend/Dockerfile"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

  [[http_service.checks]]
    method = "get"
    path = "/health"
    protocol = "http"
    interval = "15s"
    timeout = "5s"
    grace_period = "30s"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[env]
  NODE_ENV = "production"
  PORT = "3001"
