/**
 * Sample identities for the results showcase (the portfolio). Each entry is one
 * person: a few casual "before" selfies and the studio headshots we generated
 * from them. This array is the single source of truth for the gallery; add more
 * identities here to grow the portfolio, no markup changes needed.
 *
 * Image fields are URLs. Drop real R2 URLs straight in. An empty string renders
 * a labelled placeholder frame so the layout holds until the real image lands.
 * Keep `after` to a handful (2 to 4 reads best in the grid).
 */
export const samples = [
  {
    name: 'Ammar Afridi',
    role: 'Founder @ Travl.ae',
    before: ['/work/before-1.jpg', '/work/before-2.jpg', '/work/before-3.jpg'],
    after: ['/work/after-1.jpg', '/work/after-2.jpg', '/work/after-3.jpg', '/work/after-4.jpg'],
  },
];
