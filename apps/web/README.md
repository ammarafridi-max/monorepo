# @headliner/web

Placeholder. The Next.js frontend (upload flow, checkout kickoff, results page)
is built in **Phase 4 (delivery + UI)**.

Before any UI, design, styling, layout, or copy work here, read the root
`BRAND.md` first. See root `CLAUDE.md` for the hard rule.

Nothing is implemented yet.
