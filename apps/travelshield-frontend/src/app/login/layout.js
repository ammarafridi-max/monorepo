export const metadata = {
  title: 'Sign In — TravelShield',
  description: 'Sign in to your TravelShield account to manage policies, track claims, and access your travel history.',
};

export default function LoginLayout({ children }) {
  return children;
}
