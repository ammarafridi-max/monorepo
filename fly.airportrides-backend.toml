# Fly.io config for airportrides-backend
# Deploy from monorepo root with: flyctl deploy -c fly.airportrides-backend.toml

app = "airportrides-backend"
primary_region = "fra"

[build]
  dockerfile = "apps/airportrides-backend/Dockerfile"

[http_service]
  internal_port = 3001
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"

[env]
  NODE_ENV = "production"
  PORT = "3001"
